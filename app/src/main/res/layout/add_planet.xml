<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:padding="24dp"
        style="?android:attr/buttonBarStyle"
        android:orientation="vertical">



        <EditText
            android:id="@+id/editTextNamePlanet"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/planete_name"
            android:ems="10"
            android:inputType="text"
            />

        <EditText
            android:id="@+id/editTextSizePlanet"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/planete_size"
            android:ems="10"
            android:inputType="number" />

        <Button
            android:id="@+id/button_add_planet"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:drawableLeft="@drawable/ic_plus"
            android:drawableTint="@color/material_on_background_emphasis_high_type"
            android:text="@string/add_planet"
            android:textColor="@color/material_on_background_emphasis_high_type"
            style="?android:attr/buttonBarButtonStyle" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>